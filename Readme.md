# Express postgres file upload download example

## Setup
* npm install
* install postgres db
* rename .env.sample file to .env file and add any missing values in the file

## Run
* npm run start
* open http://localhost:3000
